# dbt-rowlineage
A dbt adapter-agnostic plugin that generates row-level lineage by capturing input→output record mappings during model execution. Supports SQL compilation tracing, surrogate-key mapping, incremental model lineage, and export to metadata/observability platforms.
